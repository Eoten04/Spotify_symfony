{% extends 'base.html.twig' %}

{% block title %}Hello MusiqueController!{% endblock %}

{% block body %}
    <div class="search">
        {{ render(path('app_include_search_form')) }}
    </div>

    {% if tracks is not empty %}
    <h1> Résultat de la recherche</h1>
    <div>
        {% include 'components/track_results.html.twig' with {'tracks': tracks, 'search': search} %}
    </div>
    {% endif %}
{% endblock %}
