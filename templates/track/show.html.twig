<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}track{% endblock %}</title>
        {% block stylesheets %}
        {% endblock %}
<style>
            body {
                font-family: Arial, sans-serif;
                margin: 40px;
                background-color: #f9f9f9;
                text-align: center;
            }
            h1 {
                color: #1DB954;
            }
            a.track-link {
                display: block;
                margin: 5px 0;
                text-decoration: none;
                color: #000;
                transition: color 0.2s ease;
            }
            a.track-link:hover {
                color: #1DB954;
            }
            .back-btn {
                display: inline-block;
                margin-top: 30px;
                padding: 10px 20px;
                background-color: #1DB954;
                color: white;
                font-weight: bold;
                text-decoration: none;
                border-radius: 25px;
                transition: background-color 0.3s ease;
            }
            .back-btn:hover {
                background-color: #17a74a;
            }
        </style>
    </head>
    <body>
        <a href="{{ path('app_track_index') }}" class="back-btn">Retour à la recherche</a>
        <h1>Résultats pour "{{ search }}"</h1>

        {% if results is defined and results|length > 0 %}
            <ul style="list-style:none;padding:0;">
            {% for item in results %}
                <li>
                    <a href="https://open.spotify.com/intl-fr/track/{{ item.id }}" target="_blank" class="track-link">
                        {{ item.name }}
                    </a>
                </li>
            {% endfor %}
            </ul>
        {% else %}
            <p>Aucun résultat trouvé.</p>
        {% endif %}

        </body>
</html>
